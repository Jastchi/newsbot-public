{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>Jastchi/renovate-configs//base.json"],
  "enabledManagers": ["pep621", "dockerfile", "docker-compose", "github-actions"],
  "lockFileMaintenance": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "Align uv Docker base tags with project.requires-python in pyproject.toml (currently <3.14). Update this regex when changing the upper bound.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["ghcr.io/astral-sh/uv"],
      "allowedVersions": "/^python3\\.(12|13)-/"
    },
    {
      "description": "Hugging Face transformers v5: blocked by optimum-onnx (transitive via sentence-transformers[onnx]) which caps transformers<4.58.0. Since 4.58.0 was never released (HF jumped to 5.0.0), this blocks all of v5. Tracking issue: https://github.com/huggingface/optimum-onnx/issues/114. When resolved, remove this rule and the <5.0.0 bound in pyproject.toml.",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["transformers"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    }
  ]
}
